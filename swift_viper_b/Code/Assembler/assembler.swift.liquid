//
//  {{ prefix }}{{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import UIKit

enum {{ module_info.name }}ModuleAssembler {
    typealias ModuleInputConfigurator = ({{ module_info.name }}ModuleInput) -> Void
    static func makeModule(_ configurator: ModuleInputConfigurator? = nil) -> UIViewController {

        let view = {{ module_info.name }}ViewController()

        let router = {{ module_info.name }}Router()
        router.view = view

        let presenter = {{ module_info.name }}Presenter()
        presenter.view = view
        presenter.router = router

        let interactor = {{ module_info.name }}Interactor()
        interactor.output = presenter

        presenter.interactor = interactor
        view.output = presenter

        configurator?(presenter)

        return view
    }

}
